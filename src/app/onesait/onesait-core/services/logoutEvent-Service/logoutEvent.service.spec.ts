import { TestBed, inject } from '@angular/core/testing';

import { LogoutEvent } from './logoutEvent.service';

describe('LogoutEvent', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [LogoutEvent]
    });
  });

  it('should ...',  () => {

  });
});
