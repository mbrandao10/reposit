import { TestBed, inject } from '@angular/core/testing';

import { InfoHeaderService } from './info-header.service';

describe('InfoHeaderService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [InfoHeaderService]
    });
  });

  it('should ...',  () => {

  });
});
