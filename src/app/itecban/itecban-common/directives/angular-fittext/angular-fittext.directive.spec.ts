import { AngularFittextDirective } from './angular-fittext.directive';

describe('AngularFittextDirective', () => {
  it('should create an instance', () => {
    const directive = new AngularFittextDirective();
    expect(directive).toBeTruthy();
  });
});
