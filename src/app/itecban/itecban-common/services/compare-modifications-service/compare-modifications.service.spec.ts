import { TestBed, inject } from '@angular/core/testing';

import { CompareModificationsService } from './compare-modifications.service';

describe('CompareModificationsService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CompareModificationsService]
    });
  });

  it('should ...', () => {
  });
});
